//  USBInterfaceApp
//
//  Created by 卞泽宇 on 2024/5/22.
//

/*
import SwiftUI

struct MainView: View {
    @EnvironmentObject var appStatus : AppInformation
    var body: some View {
            VStack {
                Text("Start a touch")
                    .font(.title)
                    .multilineTextAlignment(.center)
                    .bold()
                Text("Switch to devices interface to start a touch with our provided bluetooth microcontroller. After you connected with it, the microcontroller will start lightning, and data will be read.")
                    .multilineTextAlignment(.center)
                    .bold()
                .padding(.bottom, 100.0)
                .background(.white)
            }
    }
    func launch(){
        print("launch to the operation page")
    }
}


#Preview {
    MainView()
        .environmentObject(AppInformation())
}

*/
